unclassified:
  globalLibraries:
    libraries:
      - name: "test-library"
        retriever:
          oras:
            containerRef: localhost:8080/test-library
            credentialsId: someCredentials
